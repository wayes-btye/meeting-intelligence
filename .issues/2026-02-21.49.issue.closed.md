# [\#49 Issue](https://github.com/wayes-btye/meeting-intelligence/issues/49) `closed`: feat: RAG parameter display and tweakable sliders in chat UI
**Labels**: `enhancement`, `pipeline`, `ui`, `post-mvp`


#### <img src="https://avatars.githubusercontent.com/u/151667361?v=4" width="50">[wayes-btye](https://github.com/wayes-btye) opened issue at [2026-02-21 01:45](https://github.com/wayes-btye/meeting-intelligence/issues/49):

## Problem
All retrieval parameters are hardcoded and invisible to the user. For a demo/assessment this is a missed opportunity to show how the system works.

## Current hardcoded values
| Parameter | Value | Location |
|-----------|-------|----------|
| Embedding model | \`text-embedding-3-small\` | \`search.py\` |
| \`match_count\` | 10 | \`search.py:25\` |
| \`vector_weight\` | 0.7 | \`search.py:55\` |
| \`text_weight\` | 0.3 | \`search.py:56\` |

## Phase 1 — Display only (low effort)
Show a parameter info block below the retrieval strategy radio buttons:
- Semantic: \`model: text-embedding-3-small · top_k: 10 · metric: cosine\`
- Hybrid: \`vector_weight: 0.7 · text_weight: 0.3 · top_k: 10\`

Add a tooltip (\`?\` icon) explaining each strategy in plain English.

## Phase 2 — Tweakable sliders (medium effort)
Expose \`vector_weight\`, \`text_weight\`, and \`match_count\` as UI controls. Requires the API to accept these as optional query params (currently hardcoded in \`search.py\`).

## Also in scope
- Show meeting title on source cards when querying "All meetings" (see #43)
- Show \`model\` name and token \`usage\` from API response below the answer
- Empty-state message when structured lookup returns no chunks: "Answer from structured database — no transcript chunks retrieved"

## Effort
Phase 1: Low — pure frontend display.
Phase 2: Medium — API param threading + slider components.

#### <img src="https://avatars.githubusercontent.com/u/151667361?v=4" width="50">[wayes-btye](https://github.com/wayes-btye) commented at [2026-02-21 22:11](https://github.com/wayes-btye/meeting-intelligence/issues/49#issuecomment-3939588074):

Phase 1 implementation complete on branch `feat/49-rag-param-display`.

**What was implemented:**
- Parameter info block below strategy selector shows model/weights/top_k per strategy, updates reactively on switch
- `title` attribute tooltip explanations added to Semantic and Hybrid strategy labels
- Model name + token usage (input/output) displayed below answers when the API returns them
- Empty-state message shown when answer comes from structured DB (zero chunks retrieved)
- Phase 2 placeholder comment left in code where sliders would go

**Type improvement:** `QueryResponse.usage` tightened from `Record<string, unknown>` to a typed `UsageInfo` interface with `input_tokens` and `output_tokens` number fields — avoids type assertions in JSX.

`npm run build` clean, all 117 tests passing (no backend changes).

PR #60 open for review — worktree at `C:\meeting-intelligence-wt15-issue-49` intact.


-------------------------------------------------------------------------------



[Export of Github issue for [wayes-btye/meeting-intelligence](https://github.com/wayes-btye/meeting-intelligence). Generated on 2026.02.24 at 04:20:21.]
